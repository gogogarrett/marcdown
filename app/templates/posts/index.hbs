<div class="col-md-3">
  <ul>
    {{#each post in controller}}
      <li>
        {{#link-to 'post.index' post}}
          {{format-markdown post.title}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>

  {{#link-to 'posts.new'}} New {{/link-to}}
</div>


<div class='col-md-9'>
  {{outlet}}
</div>
